import Theme from '../Theme/theme';

function header(){
    return (
        <header>
            <Theme>Mode</Theme>
            <h1 className="App-header">
                MYNT Technology
            </h1>
        </header> 
    );
};

export default header;
